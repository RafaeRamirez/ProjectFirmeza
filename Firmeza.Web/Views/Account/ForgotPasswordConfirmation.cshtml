@model Firmeza.Web.Models.ViewModels.ForgotPasswordConfirmationViewModel
@{
    ViewData["Title"] = "Correo enviado";
}
<div class="container mt-5" style="max-width:480px;">
  <div class="card p-4 text-center">
    <h2>Revisa tu correo</h2>
    <p class="mt-3">Si la dirección existe en nuestro sistema, recibirás un mensaje con instrucciones para restablecer tu contraseña.</p>
    @if (!string.IsNullOrWhiteSpace(Model?.ErrorMessage))
    {
        <div class="alert alert-warning text-start" role="alert">
            <strong>Aviso:</strong> @Model.ErrorMessage
        </div>
    }
    @if (Model?.ShowResetLink == true && !string.IsNullOrWhiteSpace(Model.ResetLink))
    {
        <div class="alert alert-info text-start" role="alert">
            <strong>Entorno de desarrollo:</strong> Usa este enlace directamente para probar el flujo.<br />
            <a href="@Model.ResetLink">@Model.ResetLink</a>
        </div>
    }
    <a asp-action="Login" class="btn btn-primary mt-3">Volver al inicio de sesión</a>
  </div>
</div>
